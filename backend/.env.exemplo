# Copie para .env (dev local) ou .env.producao (docker) e preencha.
DB_HOST=201.157.230.137
DB_PORT=1521
DB_SERVICE_NAME=CMT9GE_202454_W_high.paas.oracle.com
DB_USER=AUTOMACAO
DB_PASSWORD=___
DB_OWNER=U_CMT9GE_WI

# python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=___

ADMIN_EMAIL=admin@h4c.sys
# python scripts/hash_password.py "<senha>"
ADMIN_PASSWORD_HASH=___

# Em producao inclua a origem publica do frontend
CORS_ORIGINS=http://localhost:5173,http://localhost:8100

# --- Assistente de ajuda (opcional) ---------------------------------------
# Sem chave, o botao de ajuda responde com o texto oficial do catalogo:
# custo zero e nenhum dado saindo da rede. Preencha so quando quiser ligar a IA.
# ANTHROPIC_API_KEY=
# ASSISTENTE_MODELO=claude-haiku-4-5-20251001
# ASSISTENTE_MODELO_DIFICIL=claude-sonnet-5
# ASSISTENTE_TETO_USD_DIA=1.00
